{
  "name": "drive-errors-android",
  "version": "1.0.0",
  "private": true,
  "description": "Android APK wrapper for the VFD error reference",
  "scripts": {
    "catalog": "python scripts/build_web_catalog.py",
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/core": "8.4.2"
  },
  "devDependencies": {
    "@capacitor/assets": "3.0.5",
    "@capacitor/cli": "8.4.2"
  }
}
